# 念念 AI 前端阶段施工图

## 目的

本文件把前端建设从“边做边猜”改为可见、可验收的阶段交付。每个阶段只处理一个用户结果，开始前有目标图和状态合同，完成后有真实浏览器证据。生产代码不得跳过阶段目标，HTTP `200`、静态截图或单元测试都不能单独代表完成。

## 权威输入

- 产品范围：[PRODUCT.md](PRODUCT.md)
- 视觉与交互：[DESIGN.md](DESIGN.md)
- 三站研究：[docs/frontend-reference-study.md](docs/frontend-reference-study.md)
- 目标界面：[docs/frontend-targets](docs/frontend-targets/)
- 继承矩阵：[docs/frontend-inheritance-matrix.md](docs/frontend-inheritance-matrix.md)
- 工程边界：[AGENTS.md](AGENTS.md)

## 技能路线

```text
当前实现和真实浏览器证据
-> Hallmark study：references/study.md（仅在参考/重设计时提取设计 DNA）
-> UI/UX Pro Max：Workflow Step 1-4（转成念念设计系统与栈规则）
-> Impeccable：shape（阶段设计简报）
-> 实现与真实数据接线
-> Impeccable：audit / polish（桌面与移动端质量门）
-> 阶段验收证据
```

普通缺陷修复不重复跑完整三技能；只在改变页面结构、设计系统或参考派生方向时使用完整路线。所有念念网站实现、GitHub 和发布工作继续遵守 `niannian-web-development` Skill 的 Workflow 与最小质量门。

## 阶段总览

| 阶段 | 用户结果 | 对应目标图 | 状态 |
| --- | --- | --- | --- |
| 0. 前端权威基线 | 开发前看见最终界面、边界和施工顺序 | 全部目标图 | 已完成 |
| 0.5. 主站导航与工作台四模块 | 六项导航可用，四个创作入口可进入并在真实状态上继续 | 06 | 已完成 |
| 1. 生成闭环 | 单个镜头可检查、生成、预览、采用、重试 | 02、03 | 下一阶段 |
| 2. 素材真相 | 素材可查、可复用、可追踪到镜头和节点 | 04 | 未开始 |
| 3. 项目控制中心 | 从项目库识别状态并继续正确任务 | 01 | 进行中（项目库通告单看板） |
| 4. 移动端连续性 | 手机可查看结果并完成关键确认 | 05 | 未开始 |
| 5. 完整转绘短剧验收 | 从真实输入连续推进到可下载成片 | 00-08 + 现有 Step01-Step04 | 未开始 |

## 当前状态快照（2026-08-10）

线上 `ai.cauai.fun` 已部署并验证的阶段 0.5 状态：

- 六项导航（首页、工作台、导演台、项目管理、团队管理、使用文档）在桌面真实浏览器可见可点，路由可刷新（`#workbench`、`#director-desk`、`#projects`、`#team`、`#docs`）。
- 工作台四入口均进入真实页面：无限画布 `/studio/#/studio`、一键转绘 `#redraw-intake`、一键制剧创建弹窗、智能剪辑 `https://edit.cauai.fun/`。
- 项目管理列表与统计一致：当前账户 5 个转绘项目全部可见（此前列表只渲染小说项目导致统计 5/列表空，已修复），搜索与状态筛选可用。
- 线上四个关键文件（index.html、sw.js、mvp-step02-r13.js、product-system.css）与本地 main 哈希一致；服务健康接口正常。
- 交付记录：PR #52（六项导航与一级页）、PR #54（项目管理列表修复）、PR #55（app-shell 缓存版本升级）。
- 阶段 3 进行中：项目管理页已重做为“生产通告单”看板（紧凑头部+唯一主动作、四段可点击计数条、类型分段+状态筛选、通告单式行表），本地真实浏览器已验证桌面 1440×900 与移动 390×844 无横向溢出，搜索/筛选/继续动作通过；目标图已由 yunfei-hd 2K 重绘入库。

本地 main 是当前唯一发布基线；团队管理与使用文档的一级页已上线，目标内容与后端能力仍按阶段 0.5 待产品确认后补充。

## 阶段 0：前端权威基线

### 目标

在写生产 UI 前，把产品事实、参考取舍、目标结构、响应式行为、阶段顺序和验收证据沉淀到仓库。

### 已交付

- 三站 Hallmark 研究与明确的采用/拒绝项。
- `PRODUCT.md`、`DESIGN.md`、本施工图和 `AGENTS.md` 阶段门。
- 9 张权威目标图及可复现 HTML/截图脚本。

### 完成证据

- 所有文档链接可解析。
- 目标 PNG 非空且尺寸分别为 `1440x900` 和 `390x844`。
- 图中出现项目身份、真实状态、下一动作和结果证据，不出现 Provider 密钥或内部实现信息。

## 阶段 0.5：主站导航与工作台四模块

### 用户结果

用户从顶部导航进入工作台，看到四个等权创作入口：无限画布、一键转绘、一键制剧、智能剪辑。每个入口都进入真实页面或明确能力状态，不再出现入口可用但页面/能力缺失的断裂。

### 最小可执行范围

1. 主站导航调整为固定六项：首页、工作台、导演台、项目管理、团队管理、使用文档；保留同一选中态、路由语义和移动端折叠。
2. 工作台四入口按真实能力接线：
   - 无限画布：保持 `/studio/#/studio` 路由，先解决文本模型接入与单节点生成失败，使节点可检查、提交、回读、采用。
   - 一键转绘：保持 Step01-Step04 合同，从授权参考视频连续推进到可播放、可下载成片。
   - 一键制剧：保持小说/剧本创建入口与 N00-N07 链路，推进服务端完整执行闭环。
   - 智能剪辑：保留现有编辑器入口；未确认是否站内化前只显示真实能力状态。
3. 团队管理与使用文档入口先落地导航；目标页面内容与后端能力待产品确认后按同一阶段补充。

### 技能与规则

- `niannian-web-development`：Workflow + `references/quality-gates.md`，负责导航与工作台的实现和发布证据。
- `Impeccable shape`：工作台结构变更前确认 Operate 模式简报。
- `Impeccable audit / polish`：实现后检查六项导航、四入口状态、移动端折叠与真实数据接线。
- `AGENTS.md`：受保护画布、真实项目事实、无秘密边界；不因目标图虚构 Provider 能力。

### 验收

- 六项导航在桌面与移动端可用，顺序与选中态正确，路由可刷新。
- 四个工作台入口分别进入真实页面或明确能力状态，不出现空白页或假成功。
- 无限画布单节点真实提交后可刷新回读；转绘与制剧入口保留现有已确认合同。
- `1440x900` 与 `390x844` 无关键遮挡；四入口不因导航扩展丢失。

### 完成证据（2026-08-10）

- 真实浏览器（公网 `ai.cauai.fun`）逐项点击六项导航与四个工作台入口，路由与页面内容符合本阶段用户结果。
- 项目管理页统计 5 / 列表 5 个转绘项目，搜索“1112”过滤到 1 项、清空后恢复 5 项（PR #54 修复 + PR #55 缓存版本升级）。
- CI：PR #52、#54、#55 均通过 Local contract tests 与 Clean-browser Studio smoke test 后合并；本地质量门 15 个合同测试通过。
- 本阶段“团队管理与使用文档目标内容”属于已知待确认项，不视为完成阻塞。

## 阶段 1：单镜头生成闭环

### 用户结果

用户在当前画布选中一个视频节点后，能看到输入是否完整，提交真实任务，离开并刷新页面，回来后预览真实结果，选择采用或重新生成，并从项目中下载或继续下一镜头。

### 最小可执行范围

1. 对照 [Studio 目标图](docs/frontend-targets/02-studio-canvas-desktop.png) 固定项目身份、保存状态、节点状态和右侧检查器。
2. 把文本、角色、关键帧、规格和任务 ID 绑定到当前项目实体，不保存在 DOM 或临时内存中冒充持久化。
3. 对接现有文本、Image2 和 H3 适配，前端只消费统一业务状态：未就绪、可提交、排队、生成中、需要处理、完成、失败。
4. 对照 [生成检查器目标图](docs/frontend-targets/03-generation-inspector-desktop.png) 实现可播放结果、输入证据、质量检查、采用、重试和下载。
5. 用真实浏览器执行“提交后刷新”和“完成后采用”两条路径；Provider 未完成时使用合同测试，不伪造生成成功。

### 技能与规则

- `niannian-web-development`：Workflow + `references/quality-gates.md`，负责最小实现与发布证据。
- `Impeccable shape`：阶段开始时确认 Operate 模式简报，不改产品范围。
- `Impeccable audit / polish`：实现后检查层级、状态、溢出、键盘和目标图差异。
- `AGENTS.md`：受保护画布、同一模块身份、真实项目事实和无秘密边界。

### 验收

- 清洁浏览器可以打开项目、选中节点、看到真实输入完整性并提交任务。
- 刷新后任务仍处于正确状态，完成结果可播放并属于同一项目。
- 失败保留素材和输入，页面给出可执行重试或修复动作。
- `1440x900` 和 `390x844` 无关键遮挡；移动端可查看与确认但不强行编辑完整画布。

## 阶段 2：素材真相

### 用户结果

用户能按当前项目找到角色、场景、参考片段、关键帧和采用结果，知道每项素材被哪里使用，并安全地添加、替换、下载或删除。

### 最小可执行范围

1. 统一素材业务模型：项目、类型、规格、来源验证、确认状态、使用引用和可读取地址。
2. 对照 [素材库目标图](docs/frontend-targets/04-asset-library-desktop.png) 实现搜索、类型筛选、项目统计和高密度网格。
3. 从画布打开素材选择器时保持当前节点上下文，采用后立即回读节点引用。
4. 删除前展示受影响引用；失效素材以明确状态显示，不静默移除。

### 技能与规则

- `UI/UX Pro Max Workflow Step 3-4`：素材规模、筛选和 React 稳定 key/懒加载规则。
- `Impeccable audit / polish`：检查卡片层级、内容溢出、空/加载/失败状态。
- `AGENTS.md`：不保存媒体字节、签名链接、用户媒体或 Provider 原始响应到 Git。

### 验收

- 0、100 和 1,000 项素材数据量下都能搜索、筛选和打开预览。
- 画布添加/替换素材后刷新仍指向同一服务端资产。
- 删除受引用素材必须阻止或明确完成引用迁移。

## 阶段 3：项目控制中心

### 用户结果

用户打开念念后能在首屏看到四个入口、最近项目、进行中任务、待确认事项，并一步继续最需要处理的工作。

### 最小可执行范围

1. 对照 [项目库目标图](docs/frontend-targets/01-project-hub-desktop.png) 重组现有项目库，不改变四个入口与项目创建合同。
2. 项目列表由服务端项目、任务和待确认数据组成；本地项目恢复仅作辅助。
3. 为长列表增加搜索、状态筛选和稳定行布局；项目封面来自真实项目素材或中性类型预览。
4. 明确空项目、无最近项目、任务失败和项目不可读状态。

### 技能与规则

- `Hallmark study / references/study.md`：仅复查三站首屏入口和密度证据。
- `UI/UX Pro Max Workflow Step 1-4`：项目工作台的信息架构与响应式规则。
- `Impeccable shape / audit / polish`：先确认结构，再检查实现。

### 验收

- 用户不打开项目详情也能识别进行中和待确认项目。
- 继续按钮进入正确项目与阶段，不创建重复项目。
- 长项目名、无封面和失败状态不改变列表几何。

## 阶段 4：移动端连续性

### 用户结果

用户在手机上能识别项目与镜头、查看任务状态、播放结果、完成采用/重试，并安全回到项目，不需要操作缩小版桌面画布。

### 最小可执行范围

1. 对照 [移动端目标图](docs/frontend-targets/05-studio-mobile.png) 建立线性任务视图。
2. 保留项目身份、阶段进度、真实状态、结果预览、质量证据和唯一主动作。
3. 验证触控目标、长文本、系统字体缩放、软键盘、焦点和减少动态效果。

### 技能与规则

- `UI/UX Pro Max`：UX、Typography 与 React 无障碍规则。
- `Impeccable audit / polish`：`390x844` 与中间断点检查。
- `AGENTS.md`：移动端不改变项目事实或静默触发付费生成。

### 验收

- `390px` 无横向滚动、遮挡或不可触达主操作。
- 手机采用结果后，桌面刷新显示同一采用状态。
- 动态状态被屏幕阅读器播报，所有核心动作可由键盘或触控完成。

## 阶段 5：完整转绘短剧验收

### 用户结果

用户使用真实授权参考视频完成 Step01 分析、Step02 镜头方案、Step03 资产、Step04 视频生成与采用，并获得网站可读取、可播放、可下载的转绘短剧成片。

### 最小可执行范围

1. 从合并后的 `main` 建立单一候选，使用真实项目和真实服务端状态。
2. 先跑不产生费用的合同、持久化、浏览器和恢复测试，再按既有授权做最短 5 秒 Provider 试单。
3. 验证每一步的输入来源、状态回读、结果投影、失败恢复和下一步连接。
4. 桌面完成全流程，移动端完成状态查看和结果确认。
5. 只有全路径证据成立后才能进入部署候选；部署仍遵守当前授权与发布门。

### 验收

- 清洁浏览器从新项目进入并完成单个真实镜头闭环。
- 任务生成期间关闭页面，重新进入后状态和结果不丢失。
- 采用结果可以进入下一镜头并最终汇总为可读取成片。
- 真实费用、Provider 回执和内部错误只记录在安全运行层，不暴露在页面或 Git。

## GitHub 高星工具门（2026-08-10 实测）

方法论外壳保持 Hallmark study → UI/UX Pro Max → Impeccable shape/audit/polish。
每个阶段在实现与验收时必须绑定下方对应工具门，命令与输出证据写入该阶段 Issue/PR；工具只作质量门与证据增强，不强制改造技术栈（保持原生 HTML/CSS/JS + 现有 server）。

| 工具 | 星标 | 最近推送 | 用途 |
| --- | --- | --- | --- |
| microsoft/playwright | 94k | 2026-08-10 | 真实浏览器验收（桌面/移动、刷新回读、任务恢复） |
| shadcn-ui/ui | 121k | 2026-08-10 | 组件与信息架构参考（仅方法论，不强制引入） |
| dequelabs/axe-core | 7.4k | 2026-08-07 | 无障碍自动审计 |
| GoogleChrome/lighthouse | 30k | 2026-08-09 | 性能与 UX 基线审计 |
| biomejs/biome + prettier | 25k / 52k | 2026-08-10 / 2026-08-09 | 格式与 lint（CI 可执行） |
| semantic-release | 24k | 2026-08-08 | 发布策略文档化与版本门 |
| gitleaks | 28k | 2026-07-29 | 发布前密钥/凭据扫描 |
| OWASP CheatSheetSeries | 33k | 2026-08-09 | 安全基线参考 |
| amzn/style-dictionary | 4.8k | 2026-08-07 | 设计令牌与状态语义化 |

### 阶段与工具门映射

| 阶段 | 必须使用的工具门 |
| --- | --- |
| 0 / 0.5 基线、导航与工作台 | playwright、axe-core、lighthouse、gitleaks |
| 1 生成闭环 | playwright、axe-core、lighthouse、semantic-release |
| 2 素材真相 | playwright（0/100/1000 数据量）、axe-core、lighthouse、style-dictionary |
| 3 项目控制中心 | playwright、lighthouse、axe-core、shadcn/ui（信息架构参考） |
| 4 移动端连续性 | playwright（390×844 视口）、axe-core、lighthouse |
| 5 完整转绘短剧验收 | 以上全部 + gitleaks + OWASP CheatSheetSeries |

## 每阶段 Issue / PR 必填

- 阶段编号和唯一用户结果。
- 对应目标图链接和本阶段允许偏离的原因。
- 影响文件、API 合同和受保护表面。
- 空、加载、进行中、完成、失败、恢复和移动端状态。
- 自动测试命令、真实浏览器截图和未验证项。
- 所用工具门、执行命令与输出证据（axe/lighthouse/playwright 报告或命令回读）。
- 是否调用真实 Provider、实际时长/费用和结果证据。

## 最终效果

完成阶段 0.5-5 后，念念不是“看起来像 AI 视频网站”，而是一个可持续使用的项目级生产工具：项目、素材、任务和结果在服务端一致；桌面能组织与生成，移动端能查看与确认；失败可恢复；每个镜头都有来源、状态、结果和采用记录；用户可以从参考视频或剧本连续推进到可播放、可下载的短剧成片。
