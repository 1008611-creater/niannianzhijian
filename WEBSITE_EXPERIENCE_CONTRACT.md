# 念念 AI 网站体验与动效合同

> **当前适用范围（2026-08-05）：** 本文的真实交付、无障碍、焦点、动效降级和媒体真实性要求仍然有效；页面地图、工作台结构、视觉色彩和已撤下入口不再作为 `ai.cauai.fun` 的产品决策依据。主站的唯一工程总图见 [`../authority/NIANNIAN_AI_ENGINEERING_AUTHORITY.md`](../authority/NIANNIAN_AI_ENGINEERING_AUTHORITY.md)。如两者冲突，以该工程权威文档和用户最新明确决定为准。

## 目标

念念 AI 是创作型工作台。视觉和动效的首要任务是让用户在项目、阶段、质量门、候选资产和下一步操作之间保持方向感，而不是把操作台做成展示型长滚动页面。

## 产品与范围

- 页面范围：`#home`、`#projects`、`#workbench`、`#script/:id/stage/:id`、`#redraw/:id/stage/:id`、`#redraw-evidence/:id/shot/:id`、`#showcase`、`#guide`、`#team`。历史 `#project/:id` 只做兼容重定向，不再渲染产品页面。
- 使用者：以项目交付为目的的短剧创作者、审核者和协作者；首页访客只需理解产品类别并进入项目创建或登录。
- 主目标：把用户从可信来源材料带到可审核的制作决策和受控执行，不把任何计划、候选或渠道准备误呈现为已生成交付。
- 主操作与成功证据：访客创建/进入项目，项目用户在独立制作台完成当前质量门。成功证据始终来自项目状态、精确候选 SHA、检查点、质量门和真实回执，不来自页面动画。
- 技术边界：原生 HTML/CSS/JS、局部 GSAP Core/Flip、首页真实循环视频与静态油画 fallback；视频失败时才启用既有 WebGL/Canvas fallback。所有交互均是真实 DOM；没有生成截图热点或用位图伪装的控制界面。

## 页面地图与信息层级

| 页面 | 用户问题 | 视觉主角 | 主操作 / 成功状态 | 优先级 |
|---|---|---|---|---|
| 首页 | 这是什么，为什么用它 | 全品类爆款AI视频一键创作主张、可变创作类型与两条真实创作入口 | 进入创作工作台 | P1 |
| 项目管理 | 当前有哪些项目，哪条链路在继续 | 小说短剧和参考视频转绘项目的类型、状态与下一节点 | 打开对应独立制作台或创建项目 | P1 |
| 项目详情 | 此项目现在卡在哪里 | 质量门、当前节点和已验证产物 | 读取下一动作，不伪造放行 | P1 |
| 创作工作台 | 现在该从哪里开始，已有项目卡在哪 | 制作路线、项目队列、待处理动作与项目动态 | 创建或进入对应独立制作台，查看真实进度 | P1 |
| 小说制作台 | 小说现在能产出什么 | 当前阶段、原文事实、候选与规格 | 当前阶段的唯一允许动作 | P1 |
| 转绘制作台 / 证据台 | 原片事实是否可消费 | 原始证据、时间线、质量门 | 仅按已放行节点推进 | P1 |
| 创作类型 | 念念 AI 能把什么样的故事做出来 | 三张经人工审核的原创关键画面：真人短剧、动画漫剧、参考视频转绘 | 打开对应的小说短剧或转绘项目创建流程 | P2 |
| 创作指引 | 每一步交付什么 | 当前步骤的交付物和进入条件 | 选择步骤或创建项目 | P2 |
| 团队管理 | 谁拥有当前工作区 | 当前账户与协作边界 | 登录或进入项目管理 | P2 |

## 视觉方向与资产

- 第一印象：安静、专业、可操作的影像生产系统，而不是营销长页、背景特效或高密度炫技画廊。工作台采用面板式创作工作区：左侧为真实工作区导航和创建入口，中部为项目画布，右侧为当前项目上下文；不使用虚构影视背景、伪造项目图片或假生产状态。
- 首页使用已验收的无水印油画循环视频与短时创作类型文本替换；视频只改变背景画面，不移动标题、CTA、入口、滚动、焦点或生产状态。已验收油画 PNG 作为 poster、减少动态和媒体失败时的静态显示。
- 视觉比例：每个工作页的项目/阶段/质量门占首要注意力；辅助说明、指标和动作处于第二层；装饰不与任务事实竞争。
- 字体与材质：中文系统无衬线、深色低反射工作区表面、桃红作为当前状态和主操作信号。标题、项目状态和质量门保持紧凑，首屏不使用海报级大字。
- 已有媒体：首页以 `assets/home/niannian-hero-oil-vortex-loop-v2.mp4` 为动态主视觉，`assets/home/niannian-hero-oil-paint-quiet-v1.png` 作为 poster 与静态兜底；候选图、证据帧和项目媒体只在其证明制作事实时展示。没有为 UI 本地修补生产图片。
- N06 视频审片器只消费项目内、经过 SHA、媒体探测和视觉校验的真实视频；测试回执、缺少收据、路径越界、哈希漂移或 QA 不完整时只显示明确空状态，不展示播放器、假缩略图或本地路径。
- 明确避免：全局平滑滚动、滚动 scrub、长 Pin、背景视频、3D 工作台、装饰粒子、自动跳页、全页位图 UI、在无来源时伪造项目图。
- D-018 永久禁止：不得再用同一张或任意静态图片的 CSS `transform`、`opacity`、`background-position`、`soft-light` 错位层来冒充液体、油彩或材质流动。r3-r5 的静态贴图流动方案均为已拒绝/已替代模式，不得回用为 Hero 或其他高质量主视觉。普通按钮反馈、页面进入和布局状态仍可使用 CSS transform/opacity；禁令只针对用贴图位移伪造材质形变。

| 资产 | 当前状态 | 用途 | 来源 / 权利 | 回退 |
|---|---|---|---|---|
| 首页油画背景 | Dola 2.0 Fast 无水印循环视频 + 既有 Shader/2D 失败 fallback | 首页唯一动态背景主角 | `#heroVideo`，蓝/紫/洋红逆时针油彩漩涡 | 视频不可用时启用既有 Shader/2D fallback；减少动态或离开首页时暂停并显示同一静态油画与可见标题/按钮 |
| GSAP Core / Flip | 本地 vendored 运行时 | 受控的阶段、选中和顶层页面连续性 | `vendor/` 固定版本 | 不加载时真实 DOM 直接可用 |
| 小说候选、首帧和转绘证据 | 项目事实资产 | 审核、连续性和质量验证 | 项目 ledger / manifest | 缺精确路径或 SHA 时阻断，不显示为候选 |
| 小说 Word 文档 | 用户选择的 `.docx` 原件 | 建立可追溯原文与首集改编输入 | 所属用户的可恢复上传会话 | 分片、整文件或解析校验失败时不创建项目 |
| 文本、质量门、状态 | 结构化项目数据 | 工作台信息层级 | 项目 API / checkpoint | 显示真实缺失或阻断状态 |

## 页面动效与平台契约

- 桌面和手机共享信息与操作顺序，手机改为单列/横向可控区域，不把桌面 hover 或大范围过渡作为必要能力。
- 所有工作台动效仅补间 `transform` 和 `opacity`；不改变 DOM 语义、按钮可达性、键盘焦点或页面滚动位置。
- 小说与转绘制作台的当前阶段按钮必须标注 `aria-current="step"`。用户用鼠标或键盘切换已放行阶段时，路由与焦点一起保留到新的当前阶段；后续异步审核包回读可以更新真实内容，但只能维持已有阶段焦点，不能把焦点主动移到页面其他位置。
- 创建转绘或小说短剧的弹窗打开时，仅在焦点仍停留在触发按钮或页面根节点时进入名称字段；用户已经把焦点移到其他字段时绝不抢回。`Esc`、取消和背景关闭后回到原触发按钮，成功创建后直接进入其真实后续流程而不回跳到旧入口。
- 每个被动效影响的页面都能在 GSAP/Flip 不可用、`prefers-reduced-motion`、隐藏标签页或慢资源下提供静态、可操作的最终状态。
- 入口动画所有权：`app.js` 管理顶层页面进入、首页视频播放/暂停与失败时 Shader/2D renderer 的单实例生命周期；`hero-oil-paint.css` 管理视频、静态油画、Canvas 层级与减少动态隐藏；`mvp.js` 管理小说/转绘制作台、候选选择与指引当前步骤；`product.css` 只在活动期间声明 `will-change`。
- 证据和候选的刷新所有权：项目 API 与既有质量门；视觉层不写入、不提升、不重做、不提交 Image2 或视频渠道。
- 离线壳所有权：`sw.js` 只预缓存应用外壳（HTML、样式、脚本、图标与本地 GSAP），绝不缓存 `/api/`、项目图片、候选媒体、视频端点或上传请求。断网时只回退可重新连接的页面壳，不能把任何缓存项目状态呈现为当前生产事实。

## 信息层级

- 每个制作阶段只有一个工作焦点和一个下一步动作。
- 项目状态、质量门和提交资格必须比装饰性内容更先被看到。
- 人物/场景/候选图用于证明当前制作事实，不作为无意义的背景装饰。
- 首页、项目管理、工作台和创作指引必须一致地区分“小说/剧本”与“参考视频”两条输入路径；它们可以汇聚到同一交付目标，但不得混写为同一上游流程。
- 项目管理必须显示两类真实项目，不以“转绘项目”代替小说项目；点击项目只能进入该项目自己的生产台，不创建或推进任何任务。

## 动效预算

- 等级：guided。
- 已采用：仅在用户切换小说/转绘制作阶段或首次进入制作台时，用 GSAP Core 对当前阶段的标题、摘要、指标、主工作区和底部操作区进行短距离顺序显现；工作台在明确进入时只对左侧导航、中部项目画布和右侧上下文栏作一次短距离顺序显现。项目卡 hover/focus 只强化当前项目，不移动布局。
- 不采用：平滑滚动、长距离 Pin、滚动 scrub、装饰粒子、自动页面滚动、主工作区背景视频或 3D。
- 原因：工作台需要低干扰、可扫描、可连续输入；用户的编辑位置、焦点和滚动位置不得被动效打断。

## 已实现动效规格

| 区域 | 目的 | 触发 | 初始到结束 | 时长/节奏 | 技术 | 移动端与低动态回退 |
|---|---|---|---|---|---|---|
| 小说/转绘阶段内容 | 建立阶段切换后的阅读顺序 | 项目或阶段键变化 | `opacity: 0, y: 12px` 到完整可见 | 0.34 秒，`power2.out`，55ms 交错 | GSAP Core Timeline | `prefers-reduced-motion` 下完全跳过；不改变滚动或焦点 |
| 小说 Stage 01 原文合同 | 先区分已锁定的原文事实与后续制作选择，避免把输入页误解为生成页 | 进入 Stage 01 | 静态合同条目随 Stage 01 进入顺序显现 | 复用阶段入场动效，不单独循环 | 真实 DOM + GSAP Core 渐显 | 小屏改为单列；低动态或 GSAP 不可用时完整静态呈现 |
| 资产审核选中态 | 让用户知道当前正在审核哪一项资产，并将注意力转入详情区 | 点击资产卡 | 同一资产的卡片 DOM 状态平滑承接，详情区从轻微下移到稳定可见 | 0.32 秒，`power2.out` | GSAP Flip + Core | `prefers-reduced-motion` 或 Flip 不可用时直接静态更新；不改变滚动或焦点 |
| 小说 Stage 03 镜头组切换 | 让用户明确知道自己正在核对哪一组首帧、事实卡、提示词和参考资产 | 主动点击底部镜头组 | 三个同一工作区的真实面板短距离承接，当前组在条带中以状态条和“当前预览”标识锁定 | 0.34 秒，`power2.out` | GSAP Flip + Core | 低动态或运行时不可用时立即静态切换；不改变滚动、焦点、候选决定或视频门 |
| 创作指引步骤 | 让用户在切换流程节点时看清当前阶段的交付物、进入条件与下一去向 | 主动点击左侧步骤 | 当前说明内容短距离淡入 | 0.24 秒，`power2.out`，35ms 交错 | GSAP Core | 仅用户点击触发；低动态或加载失败时直接替换真实 DOM；不滚动、不改焦点 |
| 顶层页面进入 | 在用户明确切换首页、项目、工作台、作品、指引或团队页面时建立阅读顺序 | 顶部导航的一级页面切换 | 当前页的标题、状态/工具区和主内容短距离淡入 | 0.30 秒，`power2.out`，45ms 交错 | GSAP Core | 只由顶层导航触发；Hash、自动刷新与制作台内部路由不播放；低动态时静态呈现 |
| 首页逆时针油彩流动 | 让原油画在保持材质、色板和中央标题暗区的同时产生持续可见流动 | 首页可见且系统未请求减少动态时播放 | 真实油画笔触围绕稳定暗中心持续逆时针卷动，标题、动态品类文案和主按钮保持固定 | 约 10 秒闭环，页面原生 `loop` | H.264 `<video>`；失败时才启用既有 Shader/2D fallback | 手机保持 `object-fit: cover` 中心裁切；`prefers-reduced-motion` 暂停并隐藏视频，显示同一静态油画 |
| 工作台三栏工作区 | 进入工作台时先建立导航、项目画布与当前项目上下文之间的阅读顺序 | 明确进入 `#workbench` 或项目数据首次渲染 | 左侧导航、中部项目画布、右侧上下文栏从 `opacity: 0, y: 14px` 到完整可见 | 0.42 秒，`power3.out`，70ms 交错 | GSAP Core Timeline | 手机、减少动态、隐藏标签页或 GSAP 不可用时直接静态呈现；不改滚动、焦点、项目状态或布局 |
| 项目选择与检查器 | 让用户看见所选项目的真实质量门、下一步和已确认候选，而不是被直接跳走 | 用户主动选择中部项目卡或检查器标签 | 支持浏览器时，右侧检查器以同一共享元素过渡承接；不支持时用 GSAP Flip，最终回退为即时真实 DOM | 0.22-0.30 秒，`cubic-bezier(.2,.75,.28,1)` / `power2.out` | View Transitions API + GSAP Flip + Core | 减少动态、隐藏页、API 不可用或连续切换时立即静态更新；只读 N04/N05 审核包，不写入决定、任务或 provider |
| 工作台项目动态 | 让用户知道项目何时进入当前阶段、审核结论是否记录、视频是否真的具备下一条件 | 用户主动打开工作台“动态”标签 | 以只读时间线展示原文导入、当前阶段、候选决定、执行规格记录等已确认事实 | 标签切换沿用检查器承接；不自动轮播 | View Transitions API + 真实 DOM | 仅项目所属用户可读取；不显示路径、SHA、渠道、worker、thread、测试回执或伪造的质量通过 |
| N06 视频审片 | 在不误报测试文件或未校验产物的前提下查看某一视频组的真实媒体和质量证据 | 用户主动选择 V001/V002 | 视频组、播放器、回执和质量门同步替换；无真实媒体时显示稳定空状态 | 无自动播放或装饰动效；只在显式选择时替换 DOM | 原生 `HTMLVideoElement` + 受认证的只读媒体端点 | 小屏单列；减少动态时同样可审；不支持的媒体格式或无媒体时不显示播放器 |
| 全站路由与首页背景 | 保留用户正在阅读或输入的位置，同时让首页背景保持可控、可降级 | 顶部导航、Hash 路由或减少动态偏好变化 | 工作台/制作台路由静态切换；首页可见时播放唯一背景视频，离页或隐藏标签时暂停 | 无自动路由动画；视频约 10 秒闭环且首尾近似同构 | 原生 DOM 路由 + HTML Video | 工作台保持原生滚动；`prefers-reduced-motion` 下首页直接呈现静态油画，不运行背景视频 |

## 性能与无障碍

- 不使用 ScrollSmoother 或 Lenis，页面只保留浏览器原生滚动。
- 不使用 ScrollTrigger，因此没有 Pin、scrub 或重复路由触发器清理风险。
- 工作台只补间 `opacity` 和 `transform`；首页不通过 CSS 移动静态图片，不使用 blur、粒子或脚本循环。Canvas/WebGL 只在视频加载失败时作为既有回退启用。
- 禁止在自动刷新时重播相同项目/阶段的入场动画，避免用户编辑时视觉跳动。
- 资产审核只在用户主动点击候选时运行 Flip，不在状态轮询或数据同步时自动播放。
- Stage 03 只在用户主动切换不同镜头组时承接预览、事实卡、提示词和资产架；重复点击当前组、15 秒同步与内部路由不会播放，也不会改变视频任务、候选决定或页面位置。
- 创作指引只在用户明确选择不同步骤时变化，不随项目同步或页面刷新重播。
- 创作指引的小说短剧 / 参考视频转绘切换是显式按钮状态，流程、开始前准备和创建动作必须随之同步；它不改变项目、候选或任务状态。
- 顶层页面只在用户主动使用全局导航时做一次短显现；它不覆盖表单、筛选、项目刷新或制作阶段切换。
- Stage 01 原文合同仅投影现有的原文 SHA、抽取、索引、授权与事实账本状态；它不创建或暗示任何人物图、视频或渠道任务。
- 工作台、制作台和证据台的 URL 同步不能重置页面位置；只有用户主动点击顶层导航且确实切换一级页面时，才使用一次可减少动态的回到顶部行为。
- 首页以 `#heroVideo` 播放已验收的 10 秒闭环无水印油画视频；离开首页、标签隐藏或系统请求减少动态时暂停，减少动态时隐藏视频并回退为同一静态油画。只有视频加载失败时才创建既有 `#fluidCanvas` WebGL/2D fallback，避免正常路径占用额外 GPU。
- 创作类型页展示三张原创、经人工视觉检查的关键画面。真人短剧和动画漫剧按钮只打开既有小说短剧创建流程，视频转绘按钮只打开既有转绘项目创建流程；未登录时保留既有登录边界。案例选择不创建项目、不修改项目状态，也不触发任何生产渠道。
- 工作台三栏工作区只在明确进入工作台或首次写入项目列表时播放一次，不监听键盘、表单或项目轮询；任何自动同步都不会触发重渲染动画、滚动或焦点变化。
- 项目事件流使用可单调比较的修订号。页面在加载期间、隐藏期间或短暂断线重连时收到的事件不会丢失：下一次安全读取只消费最新待处理修订；已读取的旧事件不会重复触发刷新。事件同步不自动播放过渡、不改变当前项目/标签、页面位置或输入焦点。
- 小说项目在被选中时可只读请求其所属用户的 N04 审核包；工作台仅在返回精确 `imageUrl` 的已确认候选存在时显示该图片。图片、评分、候选决定和项目状态均来自已有审核包，缺失时显示空状态，绝不生成、拼接、修补或暗示媒体已存在。
- 工作台选择是可恢复路由：`#workbench/project/{projectKind}:{projectId}/tab/{overview|assets|video|activity}`。刷新、前进和后退必须保留项目与检查器标签；未知或无权限项目保持工作台空/默认状态，不读取其他账户的审核包。
- 小说项目的“动态”标签只在用户主动打开时读取其所属项目的只读事实投影。它只消费项目创建、当前阶段、已确认的视觉审核结论、候选决定与可安全确认的执行准备状态；不能显示本机路径、SHA、渠道、worker/thread、原始任务号、测试回执或把测试质量结果写成成片。项目同步或会改变这些事实的用户动作会使缓存失效；活动读取本身绝不创建、提交、重做或推进任何任务。
- Word 小说文档采用所属用户的可恢复上传会话：浏览器先校验原文件，服务端逐片校验并记录安全的连续位置；重复提交同一已确认分片不重复写入，重新选择同一份文件可续传。只有服务端重新校验整份文件、将其固定为可读来源并成功解析 Word 后，才允许创建短剧项目。文件选择、改编权确认、上传会话 ID 和任何生产授权都不进入浏览器草稿。
- 工作台仅在用户主动改变项目或检查器标签时使用渐进增强的 View Transitions API；路由写入通过 `history.pushState` 与同一次 DOM 更新完成，避免 Hash 回调的二次重绘。自动同步、SSE、轮询、返回/前进和用户输入期间均不启用该过渡。
- N06 媒体端点只能由项目所属用户读取，并在每次读取时重新验证项目边界、收据合同、文件 SHA 与字节数；前端只收到可播放 URL、媒体元数据与质量结果，不收到本机路径、凭据或原始渠道响应。
- N06 的“版本校验”只能显示“当前媒体版本已校验”或等待状态，不得将媒体 SHA 片段投影到用户界面；精确版本校验仍仅在服务端媒体端点、收据和质量门合同中执行。
- 当 Stage 04 已加载 N06 审片包时，15 秒同步和项目事件只会对比视频组的收据、媒体、QA、质量策略和阻塞状态；未变化不重绘，质量策略下拉控件正获得焦点时完全延后回读。真实 worker 回写新收据后，下一次可安全读取的同步才替换审片状态。
- 用户可见状态必须使用产品语言，不直接显示渠道名、内部技能名、`blocked_*`、`dry_run_*`、`dry-run`、`typed provider`、`confirmed_exact_sha`、路径或结果码。工作台视频标签、转绘编辑页和生产检查器都投影为“视频状态”“等待网站执行”“等待真实媒体”“已确认，可用于后续参考”“等待质量门放行”等可行动描述。
- 项目卡、工作台当前项目检查器和项目详情共用四级状态语义：已通过、处理中、等待条件、普通等待。它们由一套 CSS token 控制表面、边界、文字、强调和状态色；状态颜色不能替代文字，也不作为生产门禁本身。
- N05 候选审核和 N06 审片只复用该状态组件投影已有决定、任务回执和媒体/视觉校验。候选的“通过 / 否决 / 重做”、视频组的“规格可锁定 / 等待网站执行 / 媒体校验通过”仍只由实际用户决定、收据和质量门改变；视觉组件不能代替任何确认或提交。
- N05 资产审核与 Stage 03 分镜界面只显示角色/场景的可读名称、当前候选版本状态、评分、职责和用户动作；资产键、文件名、截断 SHA、`exact SHA` 与提示词中的内部 N04/provider 保护语不直接暴露。精确版本校验继续只在项目合同、服务端和用户确认交易中执行。
- Stage 01 原文合同只投影“当前原文已校验”、抽取、章节结构、改编权与事实账本的可读状态；不显示原文 SHA 片段或 `confirmed` 之类的存储值。原文哈希仍是服务端溯源和改编权质量门的权威证据。
- 工作台项目卡、待处理提醒和右侧概览都必须把 job 的 `next_action` 映射为用户可执行的生产语言。渠道名、服务端实现、内部画幅参数、任务规格和原始回执不能直接作为“下一步”显示；候选标签页只显示当前版本已校验，不显示 SHA 片段。
- 工作台“候选”标签必须作为只读资产浏览器，而不只放一张代表图：它列出当前小说项目可审候选、确认比例、当前选中候选的使用范围、自动评分和版本校验；用户可主动打开大图核对。大图只读取已授权的候选 URL，不展示路径、SHA、渠道或提示词，不创建任何候选决定或视频任务。
- 创建项目弹窗只能在没有任何当前焦点时进行一次首字段聚焦，并且必须使用 `preventScroll`。不得保留延时强制聚焦逻辑，否则用户开始输入其他字段后会被抢回项目名称。
- 所有按钮、表单、抽屉和候选决策继续使用真实 DOM 及键盘焦点路径。
- 新建小说短剧、新建转绘项目和 N05 否决/重做说明使用当前浏览器会话内的本地草稿恢复。只保存文字和选择值，不保存文件选择、版权确认、账号凭据、候选决定或任何生产授权；成功提交或主动取消后清除相应草稿。草稿写入失败不能阻断页面、项目或质量门。
- PWA 安装和离线状态只增强页面可达性：断网时显示非抢焦点的状态提示，恢复连接后由既有项目读取路径重新获取所属用户的真实项目投影。PWA 不会离线创建项目、续传文件、推进质量门、派发媒体或冒充后台继续生产。

## 验证要求

- 在桌面和移动端切换阶段，页面不得自动回到顶部或抢走输入焦点。
- 在 `390 x 844` 的移动端视口，小说 Stage 03 必须保留当前镜头组、首帧审核、只读提示词与参考资产架，且文档宽度不得超过视口；Stage 04 必须保留 V001、720p 质量策略和真实媒体空状态，未产生已校验媒体时不得渲染播放器。
- 在桌面与 `390 x 844` 的移动端打开工作台深链接 `tab/activity` 时，时间线必须由项目所属用户的只读事实渲染、无水平溢出，并且不展示路径、SHA、渠道、worker、thread 或测试任务通过状态。
- 工作台深链接 `tab/assets` 在当前项目存在候选时必须展示可选资产列表；打开/关闭大图不会改变项目路由、候选决定、视频状态、滚动位置或任何生产门，`Esc` 应关闭大图并返回触发按钮。
- Word 文档上传必须在中断、重复分片、错误哈希、跨账户读取、未完整上传、已完成会话重试和浏览器表单创建场景下保持真实状态；上传成功不能被投影为候选、视频、质量通过或交付完成。
- 开启系统“减少动态效果”时，制作台应静态呈现。
- 路由切换和 15 秒数据同步不能叠加无穷动画或残留内联样式。
- GSAP 资源由本地 `vendor/gsap-3.13.0.min.js` 提供；离线/加载失败时网站必须静态可用。
- PWA 在首次在线安装后可离线打开应用外壳；离线请求 `/api/health`、任何项目 API 与项目媒体必须失败而不是返回旧缓存，恢复在线后离线提示消失且项目读取只在已有用户会话下重新执行。

## 已确认与待验证

- 已确认：全站页面/路由、受控动效预算、移动端单列回退、用户输入不被内部 Hash 路由滚动打断、首页视频按页面可见性暂停/恢复与静态油画低动态回退、生产资产不由视觉层生成。
- 已确认：D-018 继续拒绝静态图片错位伪液体。用户在 2026-07-19 明确选择真实循环视频作为首页动态主视觉；本次 Dola 成片经无水印下载、媒体探测、逐秒联系表、逆时针运动、首尾衔接与浏览器桌面/手机验证后，取代旧 Shader 候选。Shader 仅保留为视频加载失败时的技术回退。
- 已确认：公开首页、项目页、创作指引和工作台已有桌面/移动浏览器证据；当前登录用户的真实 EP001 小说制作台 Stage 01 与 Stage 02 已完成桌面和手机布局核验；独立 Playwright 浏览器已在 `390 x 844` 验证 Stage 03 的镜头审核区与参考资产架、Stage 04 的 V001/720p/无媒体状态、工作台项目动态深链接均无水平溢出，并验证 `prefers-reduced-motion` 下首页视频暂停、隐藏与静态油画回退；首页浏览器回归还覆盖 H.264 媒体加载、帧随时间变化、离页暂停、回页恢复、手机裁切和媒体失败时 Shader fallback。浏览器端 Word 创建已验证通过可恢复上传会话进入真实 N00/N01 项目；PWA 回归覆盖离线提示、Service Worker 控制、应用外壳安装、API 离线拒绝和恢复在线后的状态提示消失；代码回归覆盖动效资产、无滚动重置、认证契约、所属用户活动读取、上传完整性与小说项目链路。
- 待验证：实际候选反馈输入跨 15 秒同步时的已登录现场行为。代码回归已验证草稿打开时 `loadProjects()` 和 N04 review hydration 均跳过制作台重渲染；它仍不能被标记为已经现场验收。
- 不可改变：制作质量门、候选确认、Image2/视频 provider、费用、打包、发送和 registry 放行仍由既有生产合同控制，不由本体验合同替代。

机器可读的来源应用与逐页覆盖记录见 `WEBSITE_VISUAL_MOTION_APPLICATION.json`。它把本合同、`web-visual-motion-planner` 的选择矩阵和实际实现/验证路径一一对应，避免将“参考过 Skill”误报为已应用。
