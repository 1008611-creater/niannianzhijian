# 念念 AI 产品权威说明

## 一句话定义

念念 AI 是面向个人创作者和小型创作团队的受控 AI 视频生产工作台：用户在同一项目内组织脚本、参考素材、角色、关键帧、镜头和视频任务，并以可确认、可恢复、可下载的真实产出推进转绘短剧与原创短剧。

## 当前产品目标

用户不需要理解 Provider、工作流节点或服务器实现。进入念念后，用户应能立即回答五个问题：

1. 我正在做哪个项目、哪个镜头？
2. 当前缺什么输入，哪些输入已经确认？
3. 任务是未开始、生成中、需要处理、完成还是失败？
4. 我现在唯一应该做的动作是什么？
5. 结果是否已经写入项目，在哪里预览、采用、重试或下载？

真正的完成不是页面出现“成功”，而是用户能在项目中读取产物、完成确认，并继续下一镜头或拿到可下载的成片。

## 用户与使用情境

### 主要用户

- 个人短剧、漫剧和动画创作者，需要以较少人工操作连续推进多个镜头。
- 2-5 人的小型创作团队，需要让项目状态、素材归属和结果确认保持一致。

### 典型使用情境

- 桌面端承担高频创作、画布编排、素材选择、参数检查和批量任务处理。
- 移动端承担查看状态、预览结果、采用候选、处理明确阻塞和继续下一步。
- 用户可能在任务生成期间离开页面，因此任务与结果必须属于服务端项目，而不是当前浏览器会话。

## 产品信息架构

### 主站导航

主站顶部导航固定为六项，顺序与路由语义如下：

- **首页** `/#home`：电影感品牌入口，主 CTA 进入工作台。
- **工作台** `/#workbench`：四个等权创作入口。
- **导演台** `/#director-desk`：3D 导演台，主站宿主导航与返回路径。
- **项目管理** `/#projects`：当前账户项目库、统计、搜索筛选与继续制作。
- **团队管理**：团队工作区入口；页面与后端能力待确认后补齐。
- **使用文档**：产品使用与制作流程文档入口；目标页面待确认后补齐。

六项导航必须保持同一位置、选中态与路由语义；移动端折叠为菜单，不改变顺序。

### 项目库

统一承载项目身份、四个现有入口、最近项目、进行中任务、待确认事项和继续制作动作。四个入口保持等权：

- 无限画布：组织脚本、素材、关键帧、图像与视频任务。
- 一键转绘：从授权参考视频进入 Step01-Step04 生产流程。
- 一键制剧：从小说或剧本进入角色、分镜与成片流程。
- 智能剪辑：外部编辑器入口，保留能力状态。

### 工作台四模块

工作台以四个等权编号入口承载创作路径，每块只承诺当前真实能力状态：

- **01 无限画布**：保留现有 `/studio/#/studio` 画布与八类节点；当前需先解决文本模型接入与节点生成失败，达到单个节点可检查、提交、回读、采用。
- **02 一键转绘**：保留现有 Step01-Step04 转绘合同；目标是用户从授权参考视频连续推进到可播放、可下载的成片。
- **03 一键制剧**：保留小说/剧本创建入口与 N00-N07 链路；目标是服务端完整执行闭环，不再依赖本地任务合同才能推进。
- **04 智能剪辑**：保留现有编辑器入口；是否改造为站内模块待产品确认，未确认前只显示真实能力状态，不虚构编辑能力。

### 创作画布

画布只表达项目关系和生产状态。节点保存项目实体引用、输入完整性、任务状态和结果引用，不保存媒体字节、密钥、私有链接或 Provider 原始响应。

转绘 Skill 的节点化生产顺序、统一节点字段、端口、预览/上传/恢复行为和阶段验收见 [画布 Skill 节点化生产计划](authority/CANVAS_SKILL_NODE_EXECUTION_PLAN.md)。该计划将原片输入、Step01-Step04、角色/场景资产、Image2、H3/Animate、外部智能剪辑和交付作为可追踪节点；只有完成真实服务端执行、结果回项目资产库和刷新恢复的节点，才能显示为可生产。

### 生成检查器

检查器是画布的真实执行面：生成前显示输入证据和输出规格；生成中显示可离开的持久任务；生成后显示可播放结果、质量检查、采用、重试和下载动作。

### 素材库

素材库按项目显示角色、场景、参考片段、关键帧、候选和采用结果，并明确素材被哪些镜头或节点使用。失效引用必须显式出现，不能用空白卡片掩盖。

### 导演台与 Step01-Step04

现有导演台和 Step01-Step04 继续承载转绘生产合同。前端统一工程不得改变其已确认业务语义、资产引用、API 映射或完成证据。

## 目标体验

权威目标界面位于 [docs/frontend-targets](docs/frontend-targets/)：

- [项目库桌面目标](docs/frontend-targets/01-project-hub-desktop.png)
- [Studio 画布桌面目标](docs/frontend-targets/02-studio-canvas-desktop.png)
- [生成结果检查器目标](docs/frontend-targets/03-generation-inspector-desktop.png)
- [素材库桌面目标](docs/frontend-targets/04-asset-library-desktop.png)
- [Studio 移动端目标](docs/frontend-targets/05-studio-mobile.png)

这些图定义信息层级、任务状态、操作位置和响应式保留内容，不代表虚构的 Provider 能力。图中的项目和状态是界面契约示例；实现必须由真实项目数据驱动。

## 内容与状态范围

### 需要稳定承载的数据

- 项目：空项目、典型 5-20 个镜头、长项目 100 个镜头以上。
- 素材：空素材库、典型 20-100 项、长项目 1,000 项以上。
- 画布：空画布、典型 10-40 个节点、复杂项目 200 个节点以上。
- 任务：无任务、单任务、并行任务、部分完成、失败后恢复、旧结果与新结果并存。
- 文本：长项目名、中文/英文混排、长错误说明和小屏换行。

### 必须具备的状态

- 首次使用、空状态、导入中、输入不完整、准备就绪。
- 排队中、生成中、可离开页面、取消中、完成。
- 需要人工确认、可恢复失败、不可恢复输入错误、权限不足。
- 结果可读取、结果不可读取、已采用、可重新生成、可下载。

所有状态都必须包含业务语言、状态图标或结构和下一动作，不能只靠颜色。

## 产品原则

1. 首页负责电影感与品牌记忆，生产页面负责消除犹豫。
2. 一个页面只突出一个当前动作，其余信息构成可扫描的证据层级。
3. 项目、素材、任务和结果以服务端事实为准；本地缓存只能帮助恢复。
4. 用户不需要看到 Provider 名、内部任务号、文件路径、SHA、密钥或原始错误。
5. 失败不能抹掉已上传素材、已完成分析或可复用结果。
6. 熟悉的工具交互优先于为了新奇而重新发明控件。
7. 移动端保留项目身份、当前状态、主要动作和结果证据，不复制完整桌面画布。

## 已确认边界

- 保留现有念念首页、Logo、当前画布、素材库、Image2、H3、导演台、Step01-Step04 和 API 适配的已确认行为。
- 不恢复旧 Nomi 自建画布、`#canvas` 或 `owned-canvas-director-import`。
- 不使用 mock、fixture、空值或静态成功态冒充真实生成。
- 不新增音频、3D、白板、全景或场景 3D 的可执行承诺，除非其服务端生产路径已经存在并通过真实验收。
- 不在源码、文档、截图或提交中保存密钥、Cookie、`.env`、签名链接、用户媒体或 Provider 原始响应。

## 成功指标

- 新用户能从项目库进入正确入口，并在一次浏览中识别当前项目和下一动作。
- 用户刷新或换设备后仍能看到同一项目、任务和已完成结果。
- 从真实输入到单个 5 秒镜头的生成、预览、采用和下载路径可以完整跑通。
- 任一失败都有可理解原因和可执行恢复动作，且不要求无故重新上传。
- 桌面与 `390px` 移动端均无关键操作遮挡、横向滚动或文本溢出。
- 键盘、焦点、对比度和动态内容通知达到 WCAG 2.1 AA 目标。

## 产品表面拓扑

念念当前拥有六个独立的既有产品表面，前端统一工程只在这些表面内部扩展，不合并、不替换：

- **主页** `/`：电影感品牌入口，黑色壳，金色/粉色导航，"开始创作"进入工作台。
- **项目管理** `/#projects`：主站深色壳，粉色眉题，四项指标，项目搜索筛选与继续。
- **工作台** `/#workbench`：四个等权编号入口卡片 — 无限画布、一键转绘、一键制剧、智能剪辑。
- **Studio 项目库** `/studio/#/studio`：浅色 Studio 视觉世界，三种开始动作，来源筛选，最近项目。
- **Studio 画布** `/studio/?step=...`：浅色画布，三模式，四类资源，八种节点，生成检查器，时间轴与 AI 侧栏。
- **3D 导演台** `/#director-desk`：深蓝黑 3D 视口 + 场景树 + 摄像机检查器，主站宿主导航与返回路径。

各表面与其已有路由、组件、API 和交互拓扑的继承级别详见 [docs/frontend-inheritance-matrix.md](docs/frontend-inheritance-matrix.md)。任何目标图若遗漏该矩阵中的受保护项，不具备前端权威性。

## 相关权威文件

- 视觉与交互合同：[DESIGN.md](DESIGN.md)
- 前端阶段施工图：[FRONTEND_EXECUTION_PLAN.md](FRONTEND_EXECUTION_PLAN.md)
- 三站参考研究：[docs/frontend-reference-study.md](docs/frontend-reference-study.md)
- 工程执行约束：[AGENTS.md](AGENTS.md)
