# 念念智剪项目工作规则

## 项目边界

- 本目录 `E:\codex\niannianai\niannianzhijian` 是念念智剪唯一的本地开发工作区。
- 念念 AI 主站仍位于 `E:\codex\aisp\aidaihuo\niannian-ai-web`；不得把主站代码、数据库或发布配置复制进本项目。
- 公网编辑器 `https://edit.cauai.fun/` 只接受本地构建、验证后的完整发行包；禁止在服务器工作目录直接开发。

## 本地 WSL 优先

- 开发、测试、联调默认在 WSL Ubuntu 中执行；Windows 入口使用 `local-wsl-dev/*.ps1`，WSL 入口使用 `local-wsl-dev/*.sh`。
- 默认端口：主站 `3026`，智剪 `5199`。本地媒体、缓存、日志和 PID 只能写入 `local-wsl-dev/data` 与 `local-wsl-dev/runtime`。
- 本地联调使用独立 SSO 密钥和 `0` 积分价格；不读取线上 `.env`、服务器媒体、生产数据库或生产 Tunnel token。
- Qwen3 ForcedAligner 在已验证的 NVIDIA 560 驱动环境中使用 `torch 2.7.1+cu126`，模型和虚拟环境默认放在 WSL 原生缓存 `$HOME/.cache/niannianzhijian/qwen-forced-aligner`，避免回退到多分钟 CPU 推理或从 `/mnt` 读取大模型。
- 本地页面必须通过 `bash local-wsl-dev/verify-local.sh`，并在真实浏览器路径确认首屏后，才允许制作发行包。

## 必须保留的能力

- MiMo ASR/TTS 共用供应商配置入口和自定义供应商能力。
- 念念智剪品牌名称与 `public/niannian-logo.svg`。
- 预览代理修复、上传/导出路径修复、SSO 和积分扣除边界。
- Qwen3 ForcedAligner：已知文案和实际音频的词级/字级时间戳对齐。
- AGPL 开源交付入口；发行包排除用户媒体、上传文件、`.env` 和任何凭据。

## 产品宿主与双模式

- 念念智剪是共用剪辑能力，不等同于任一上层业务站点。
- 念念 AI 的业务主场是 AI 转绘和 AI 短剧；接入念念智剪时，快速模式应优先提供短剧片段精修、剧情混剪、角色/画面转绘素材成片、图生视频整理和发布版包装。
- 念念智选的业务主场是电商带货；接入念念智剪时，快速模式应优先提供商品种草、测评口播、探店开箱、卖点讲解和发布版包装。
- 两个宿主都可以进入同一个念念智剪专业模式；专业模式保留当前完整时间线、Agent、素材库、字幕、配音、BGM、质检和导出能力。
- 快速模式面向普通用户展示“成片方案”，不得在 UI 中暴露 API、模型、供应商、Agent 工具名或时间线内部术语。
- 每个成片方案卡必须让用户看懂结果、适用场景、素材要求、预计时长/平台和下一步动作；不要把内部 `template` 或 `workflow` 名称直接展示给用户。
- 快速模式和专业模式必须共用同一个真实工程。快速模式生成的片段、字幕、旁白、BGM 和包装元素必须能进入专业模式继续编辑。
- 快速模式模板只编排现有剪辑、素材、转写、配音、音乐、验证和导出能力，不复制第二套时间线编辑逻辑。
- 每次快速模式运行必须记录 `recipeId`、`recipeVersion`、`styleId`、`workflowRunId`、素材源版本、创建的时间线对象和失败阶段；重跑只替换本次运行创建的对象，不覆盖用户手工修改。
- 快速模式结果必须经过真实工程结构和合成画面验证；字幕和口播必须使用真实时间戳或明确失败，禁止按文案长度伪造时间轴。

## 发布边界

1. 先在本地 WSL 构建并验证完整候选包。
2. 发布前核对源码、静态资源、版本链接和回滚链接属于同一候选。
3. 服务器只接收候选发行包；不得直接编辑 `/opt/edit-ai-openchatcut`、`/var/www`、Nginx、systemd 或 Tunnel 配置。
4. 发布后实际检查 `https://edit.cauai.fun/` 的首屏、品牌、预览、导出和控制台错误；HTTP `200` 或构建成功单独不足以宣布完成。
5. 发布后读取线上非敏感配置状态，确认预期 Agent 厂商和模型已配置并可被编辑器选择；再从真实编辑器路径发送一次只读请求，确认 Agent 实际调用工具并返回结果。旧对话中的错误消息不能替代这次新请求的验收。

## 交接

- 新主控线程应从本目录启动，并优先读取本文件、`README.md`、`ITERATION_PLAN_ZH.md` 和 `local-wsl-dev/README.md`。
- 交接时说明当前候选、验证结果、未验证的付费 API 路径以及生产线上版本未改变的事实。
