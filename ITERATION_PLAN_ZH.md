# OpenChatCut 迭代计划

目标：把 OpenChatCut 迭代成一套本地优先、可替换供应商、面向带货短视频生产的开源剪辑工作流。核心顺序是先稳定运行底座，再闭合“素材到成片”的生产链路，最后做批量、协作和插件化。

## 当前状态

| 阶段 | 状态 | 已验证结果 |
| --- | --- | --- |
| R0 运行底座 | 已完成 | Node 24、专属 npm 缓存、`npm ci`、构建和本地 `127.0.0.1:5199` 服务可重复运行；密钥只在服务端 keystore。 |
| R1 统一供应商层 | 进行中 | MiMo 已可用；OpenAI 兼容 TTS 与 OpenAI 兼容 ASR 已接入设置页、服务端密钥库、连接探测和 Agent。ASR 强制要求 `/audio/transcriptions` 返回 `verbose_json` 词级时间戳，支持官方接口、自建网关和本地兼容服务；下一步是把现有能力收敛到统一适配器注册表，并补图片、视频、音乐适配器。 |
| R2 素材与项目资产 | 进行中 | 已有批量导入、分片上传、代理/本地媒体、源版本、项目持久化、转写、镜头检测、抽帧和 CLIP 语义检索；本地 Tesseract OCR 与 OpenAI 兼容视觉分析已写入版本绑定素材智能数据，可检索商品/人物/品牌/场景标签。待补多帧聚合与本地视觉模型。 |
| R3 脚本到粗剪 | 进行中 | 新增 `assemble_rough_cut`、`render_rough_cut_voiceover`、`prepare_rough_cut_captions`、`place_rough_cut_bgm` 和 `check_rough_cut_ready`：明确素材范围会装配为独立可编辑时间线；旁白生成后用真实词级转写建立可追溯字幕轨；已完成的音乐资产可自动铺满粗剪并在旁白下闪避；导出前可检查来源、字幕、BGM 和安全区结构。无声视频的旁白粗剪工作流已默认使用 MiMo 内置音色“冰糖”，仍允许显式切换其他已配置供应商和音色。已用本地真实项目完成合成画面抽帧与导出闭环验收：规格、音频、黑帧、冻结帧、长静音和峰值检查通过，并保留可回读的成片证据。 |

## 阶段路线

### R0：运行底座

- 固定 Node `24.x`、锁文件和项目专属 npm 缓存；提供 Windows/macOS/Linux 一键启动与构建命令。
- 明确四类本地数据边界：媒体文件、项目工程、模型缓存、服务端密钥；所有写入采用临时文件加原子替换。
- 验收：新机器按固定命令可启动；项目重启后工程和素材关联不丢；`GET /api/keys` 不含任何密钥值。

### R1：统一供应商层

- 每个能力都使用适配器契约：`validate`、`generate`、`probe`、默认模型/音色、错误归一化和能力状态。
- 已覆盖官方供应商和 OpenAI 兼容 LLM；TTS 已覆盖 ElevenLabs、豆包、MiMo、MiniMax 与 `openai-tts`。
- OpenAI 兼容转写已完成服务端 multipart 上传、词级时间戳解析、设置页配置、连接探测和 Agent 工具接入；下一批补图片和视频，并将现有 TTS、ASR、视觉、音乐配置收敛到统一注册表。支持自建网关、聚合平台、转发地址和本地服务。
- MiMo 的 `mimo-v2.5-asr` 可复用现有 `MIMO_API_KEY` 与同地区 Base URL，并使用 `POST /v1/chat/completions` 发送 MP3/WAV Base64 音频；官方响应仅承诺转写文本、没有词级时间戳。因此它应作为“文本转写/素材理解”适配器接入，不能直接替代粗剪字幕和口播删词所需的词级 ASR；若要用于时间线字幕，后续必须补独立的真实强制对齐或选择带词级时间戳的 ASR。
- 验收：设置页可新增/切换供应商；浏览器只提交任务；失败可定位到供应商、HTTP 状态和可操作建议；成片记录实际模型和参数。

### R2：素材与项目资产

- 已建立 `AssetIntelligence` 数据结构，包含 `ocrText`、`tags`、`entities`、`scenes`、`thumbnailRefs`、`waveformRef`、`sourceRevision` 和分析模型版本；本地 Tesseract OCR 可通过 `analyze_asset(kind=ocr)` 写入 OCR 文本，OpenAI 兼容视觉模型可通过 `analyze_asset(kind=vision)` 写入商品、人物、品牌和场景标签。
- 导入后异步生成缩略图、波形、关键帧、镜头段；转写、OCR、商品/人物/场景分析都绑定素材源版本，重链后旧结果自动失效。视觉分析只发送用户选定的本地图片或视频抽帧，密钥只保存在本机服务端。
- 搜索统一覆盖文件名、标签、OCR、转写和视觉向量，并返回可定位的素材时间区间。
- 验收：导入一个视频后可搜索“商品展示”“人声高光”等词；重新打开项目仍能找到同一素材和分析结果；替换源文件不会复用旧分析。

### R3：脚本到粗剪

- 输入选题、商品信息和目标平台，生成脚本、分镜、镜头选择、旁白、字幕、BGM 和基础转场。
- 已具备 `assemble_rough_cut`：Agent 先通过检索、转写或抽帧选择明确的素材时间范围，再创建独立粗剪序列；每个片段保留 `sourceAssetId`、`sourceRevision`、`srcInFrame`、时长和转场，原时间线不变。
- 旁白文案作为镜头的 `roughCut` 元数据保存。`render_rough_cut_voiceover` 只在用户发起时调用已配置 TTS，生成的音频会进入媒体库、放置在新建的锚点音轨，并回写镜头到配音资产的关系；生成资产记录供应商、模型、音色、文本和速度。`prepare_rough_cut_captions` 在缺少转写时调用 ASR，并只使用返回的词级时间戳建立字幕轨；字幕轨持久化关联的旁白片段、素材和源版本，重链/失效时拒绝绑定，绝不按文案长度伪造时间轴。已知旁白文案与音频无法完整逐词/逐字对齐时，整段字幕必须失败并保持时间线不变，禁止静默丢字后继续发布。
- BGM 仍由既有 `submit_music` 异步生成和 `track_progress` 跟踪；成功素材进入媒体库后，`place_rough_cut_bgm` 才会放置到 follower 音轨，自动循环/裁切至视觉时长，保存资产与源版本，并在已存在的旁白 anchor 音轨下使用现有路由闪避。重跑只替换该工作流创建的 BGM 片段，不动手工音乐。
- `check_rough_cut_ready` 是只读结构预检：检查视觉时长、不可用媒体、旁白/字幕关系、词级转写、BGM 覆盖与源版本，以及既有字幕安全区规则。它不会冒充画面或音频采样质检；导出前仍需 `view_timeline_frames` 检查合成画面，导出完成后使用已有 `verify_export` 检测黑帧、冻结帧、静音、响度、规格和转场证据。
- 每个镜头和音频片段保存来源：脚本文本、素材 ID/源版本、模型、提示词和生成任务 ID。
- 验收：给定素材和目标，单次工作流生成可播放粗剪；每一步可单独重做并保留工程可编辑性；真实口播对齐覆盖全部文案单位，任一缺失、错序、重叠或非正时长结果都阻止字幕写入。

### R4：智能剪辑

- 句子级文字稿编辑驱动音画同步；删除静音、停顿和口头禅；语义匹配 B-roll；节奏卡点；音量闪避；横竖版自动裁切。
- 时间线使用稳定的 source range 和 revision 引用，文案变更通过可回放的编辑计划重建，而不是直接覆盖不可追溯的结果。
- 验收：删一句文案后对应字幕、旁白、镜头和 BGM 同步变化，无重叠、黑帧或错位。

### R5：带货工作流

- 商品信息抽取、卖点库、开头钩子库、痛点/利益点/行动号召模板。
- 支持口播、混剪、测评、对比、开箱等模板；同一商品批量生成不同钩子、配音和节奏的版本。
- 验收：一次输入可产出多条独立可编辑工程，并能比较每条脚本、素材、模型和导出参数。

### R6：质检与导出

- 导出前检查黑帧、静音、字幕越界、字幕/配音不一致、画面/旁白错位、响度和素材缺失。
- 输出 H.264/H.265，横屏/竖屏，多码率，带字幕/外挂字幕/无字幕版本。
- 验收：质检结果可解释；导出文件可播放、音画同步、字幕不溢出且符合目标平台规格；字幕必须覆盖已批准文案的全部对齐单位，供应商返回部分时间戳时导出前明确报错而不是产出缺字成片。

### R7：批量与协作

- 项目模板、品牌资产、账号风格预设、任务队列、批量渲染、版本对比、审批状态；可选接入对象存储和远程渲染。
- 验收：同一项目批量产出多个版本，每条结果可追溯完整生产输入。

### R8：产品化扩展

- Provider SDK、工作流模板 SDK、MCP 工具扩展、可配置 Agent、权限/配额、运行日志和成本统计。
- 新增供应商或工作流不修改核心时间线；第三方扩展通过版本化契约和验证套件接入。

## 技术边界

```text
编辑器层：时间线、预览、字幕、素材库、导出
工作流层：脚本、粗剪、配音、字幕、带货模板、质检
Provider 层：官方 API、OpenAI 兼容 API、转发地址、本地模型
基础设施层：本地文件、项目存储、密钥库、任务队列、缓存、日志
```

所有供应商请求都从服务端发出。API Key 只写入 `.env.local`/服务端 keystore；浏览器只能看到 `configured`、模型列表和连接结果。每次生成任务记录供应商、模型、参数、素材源版本、提示词和导出规格，保证结果可复现。

## 近期执行顺序

1. 完成 R1 适配器注册表，并补 OpenAI 兼容转写/图片/视频的连接测试和错误归一化。
2. 在现有导入、转写、抽帧、镜头检测和语义检索之上继续接入商品/人物/场景标签，写入既有 `AssetIntelligence`。
3. 打通“导入素材 -> 分析 -> 脚本 -> 粗剪 -> MiMo/自定义 TTS -> 真实字幕 -> BGM -> 时间线质检 -> 导出”单条闭环。
4. 再增加带货模板和多版本批量生产；质检通过后才扩展云渲染与团队协作。
