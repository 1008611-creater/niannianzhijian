[Service]
# The Step01 executor reads the existing non-secret Step03 Responses profile.
# The key remains only in this root-owned environment file.
EnvironmentFile=-/etc/niannian-ai/step03-worker.env
Environment=NIANNIAN_STEP01_AUTO_EXECUTE=on
